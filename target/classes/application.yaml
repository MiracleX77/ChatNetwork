spring:
  jpa:
      hibernate:
        ddl-auto: update
  datasource:
      url: jdbc:postgresql://localhost:5432/network-backend
      username: postgres
      password: syspass
  servlet:
      multipart:
          enabled: true
          max-file-size: 2MB
          max-request-size: 5MB
---
app:
    token:
          secret: miraclex77
          issuer: BackendService

